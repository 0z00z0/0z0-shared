namespace ZeroZero.Tray;

/// <summary>Whether the taskbar is drawn dark or light. It follows the system theme setting,
/// which is separate from the apps setting and is the one an icon sits on.</summary>
public enum TaskbarTheme
{
    Dark,
    Light,
}
