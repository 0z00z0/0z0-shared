namespace ZeroZero.Tray;

/// <summary>The tone an icon's strokes are drawn in so they read on the taskbar beneath them.
/// Which colour that is, and what is drawn, stays with the application.</summary>
public enum StrokeTone
{
    Light,
    Dark,
}
