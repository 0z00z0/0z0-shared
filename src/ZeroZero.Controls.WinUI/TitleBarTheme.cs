namespace ZeroZero.Controls.WinUI;

/// <summary>Which of the two themes a title bar is painted for. Framework-free, so the palette
/// that answers it can be tested without the XAML runtime.</summary>
public enum TitleBarTheme
{
    Light,
    Dark,
}
