<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>ZeroZero.Config</RootNamespace>
    <Version>$(ConfigVersion)</Version>

    <AssemblyTitle>ZeroZero.Config</AssemblyTitle>
    <Product>ZeroZero.Config</Product>
    <Description>Atomic JSON settings files: typed snapshot reads, mutation under one lock, change notification and quarantine of an unreadable file.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ZeroZero.Config.Tests" />
  </ItemGroup>

</Project>
